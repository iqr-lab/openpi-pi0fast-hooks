record:
  enabled: true
  dir: /nfs/roberts/scratch/pi_tkf6/as4643/policy_records
  add_timestamp: true

hooks:
  enabled:
    - observation_input
    - token_spans
    - prefix_embeddings
    - prefix_final_hidden_state
    - prefix_gradients
    - raw_attention_weights
    - value_vectors

  action_chunks:
    num_chunks: 1

  raw_attention_weights:
    layers: [1, 16]

  value_vectors:
    layers: [1, 16]
